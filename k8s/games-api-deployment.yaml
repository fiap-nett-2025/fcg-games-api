apiVersion: apps/v1
kind: Deployment
metadata:
  name: fcg-games-api
  labels:
    app: fcg-games-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fcg-games-api
  template:
    metadata:
      labels:
        app: fcg-games-api
    spec:
      containers:
        - name: fcg-games-api
          image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/fcg-games-api:latest
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: AWS_REGION
              value: "${AWS_REGION}"
            - name: AWS_DEFAULT_REGION
              value: "${AWS_REGION}"
          envFrom:
            - secretRef:
                name: aws-credentials
            - configMapRef:
                name: fcg-games-config
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
